/**
 * @format
 */

import { AppRegistry } from 'react-native';
import AppWithLogin from './AppWithLogin';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => AppWithLogin);
